# Rental Prices in the Czech Republic
## Data Analysis in Python Final Project
### Authors:
Matyáš Tvrz (70038431@fsv.cuni.cz)

Jonathan Eugenio Gaeta (48207837@fsv.cuni.cz)

This is a repository for the Data Processing in Python final project created by Matyáš Tvrz and Jonathan Eugenio Gaeta. We scrape data from sreality.cz and bezrealitky.cz on rental properties in the Czech Republic. In the first part, we present an interactive heatmap (choropleth) based on the median rental prices per meter squared in each region (data on regions was downloaded from https://github.com/siwekm/czech-geojson). The map includes popups with information about specific properties including a link to the public listing. In the second part, we analyze the data. First, we present basic summary statistics of the rental prices, grouped by districts and flat types. Next, we present exploratory plots, which visually illustrate some of the features of the data. Finally, we estimate two simple regressions: a level OLS on log-level OLS. We display the results, along with diagnostic plots and a fixed effects plot for districts.

AI disclaimer: we used Anthropic's Claude for polishing pieces of code for better readability and logic, including docstrings, and for bug fixes.

### Installation of required packages:

```bash
pip install -r requirements.txt
```

## Run the interactive Streamlit app:

```bash
streamlit run streamlit.py
```

## Or continue through the main notebook...

### Known Issues:
The sreality.cz dataset cannot be updated, the API site returns a 404 error. Updating the data at this point only requests bezrealitky data.

Some variables from the old sreality.cz dataset do not fully correspond to real public listings.

## Project Structure

```
Python_final/
├── main.ipynb              # Main notebook — run analysis here
├── streamlit.py            # Interactive Streamlit app
├── requirements.txt        # Package requirements
├── README.md               # README file
│
├── src/                    # Source modules
│   ├── function_scripts.py # Scraping and other functions
│   ├── data_cleaning.py    # Data cleaning and processing 
│   ├── draw_heatmap.py     # Folium choropleth map construction
│   └── analysis.py         # Summary statistics, EDA plots, OLS models
│
└── data/
    ├── raw/                # Raw scraped data 
    │   ├── df_sreality.json
    │   ├── df_bezrealitky.json
    │   ├── kraje.json      # Czech regions GeoJSON
    │   ├── okresy.json     # Czech districts GeoJSON
    │   └── obce.json       # Czech municipalities GeoJSON
    │
    └── processed/          # Cleaned data generated by data_cleaning.py
        ├── df_all.csv
        ├── df_reg.csv
        ├── df_heatmap.parquet
        ├── df_sreality_property.parquet
        └── df_bezrealitky_property.parquet
```

